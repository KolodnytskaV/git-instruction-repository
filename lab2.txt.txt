#include"all_samples.h"

using std::string;
using std::cout;
using std::endl;

void studentInformatiomModel()
{
       //ПІБ
       string studentFullName = "Студент 1":
       //Ідентифікатор студента/залікової книжки
       unsigned int studentID = 1;
       //Вік учня
       unsigned short int studentAge = 22;
       //Поточний рік навчання (клас)
       unsigned short int currentStudyYear = 1;
       //Чи навчається учень?
       bool isStudying = true;
       //Заклад освіти
       string universityName = "НПУ ім. Драгоманова"
       //Адреса учня
       string originAddress = "Чернігів, Україна";
       //Освітній ступінь студента
       string degree = "Магістратура";
       //Навчальна група
       string group = degree + to_string(currentStudyYear);
       
       unsigned int totalMemoryInBytes = sizeof(studentFullName) +
                                         sizeof(group) +
                                         sizeof(studentID) +
                                         sizeof(studentAge) +
                                         sizeof(currentStudyYear) +
                                         sizeof(isStudying) +
                                         sizeof(originAdress) +
                                         sizeof(universityName) +
                                         sizeof(degree);
        cout << "ПІБ студента:\t\t" << studentFullName <<endl;
        cout << "Група:\t\t\t" << grop << endl;
        cout << "Номер залікової книжки:\t" << studentID << endl;
        cout << "Вік студента:\t\t" << studentAge << endl;
        cout << "Рік навчання:\t\t" << currentStudyYear << endl;
        cout << "Чи ще навчається:\t" << ((isStuding) ? "Так" : "Ні") << endl;
        cout << "Адреса:\t\t\t" << originAdress << endl;
        cout << "Заклад освіти:\t\t\t" << universityName << endl; 
        cout << "Освітній ступінь:\t" << degree << endl;
        
        cout << "Загальний об'єм використаної пам'яті (в байтах):\t" << 
                 totalMemoryInBytes << endl;
